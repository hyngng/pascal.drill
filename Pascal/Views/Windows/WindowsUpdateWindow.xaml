<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Pascal.Views.SubPages.WindowsUpdateWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Pascal.Views.SubPages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="#4978d0">
        <StackPanel HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Spacing="8">
            <Image Source="ms-appx:///Assets/Windows-loading-cargando.gif"
                   HorizontalAlignment="Center"
                   Stretch="None"/>
            <TextBlock Text="This window is running in Fullscreen mode"
                       Style="{ThemeResource TitleTextBlockStyle}"
                       TextAlignment="Center" />
            <Button x:Name="Close"
                    Click="Close_Click"
                    Width="200"
                    HorizontalAlignment="Center">
                <StackPanel Orientation="Horizontal"
                            VerticalAlignment="Center">
                    <SymbolIcon Symbol="Cancel"
                                Foreground="{ThemeResource SystemFillColorCriticalBrush}"
                                Margin="0,0,4,0" />
                    <TextBlock Text="Close"
                               Foreground="{ThemeResource SystemFillColorCriticalBrush}" />
                </StackPanel>
            </Button>
        </StackPanel>
    </Grid>
</Window>
