<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Drill.Pages.CreditPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Drill.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:toolkit="using:CommunityToolkit.WinUI.Controls"
    mc:Ignorable="d">

    <Page.Resources>
        <x:Double x:Key="SettingsCardSpacing">4</x:Double>

        <Style x:Key="SettingsSectionHeaderTextBlockStyle"
               BasedOn="{StaticResource BodyStrongTextBlockStyle}"
               TargetType="TextBlock">
            <Style.Setters>
                <Setter Property="Margin" Value="1,30,0,6" />
            </Style.Setters>
        </Style>
    </Page.Resources>

    <Grid x:Name="RootGrid"
          MaxWidth="1064">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="PageHeader"
                   Margin="36,24,36,0"
                   AutomationProperties.HeadingLevel="Level1"
                   Style="{StaticResource TitleTextBlockStyle}"
                   Text="크레딧" />

        <ScrollView Grid.Row="1" Padding="36,0,36,0"
                    VerticalScrollBarVisibility="Auto"
                    VerticalScrollMode="Auto">
            <Border>
                <StackPanel Spacing="{StaticResource SettingsCardSpacing}">
                    <TextBlock Style="{StaticResource SettingsSectionHeaderTextBlockStyle}" Text="만든 이" />
                    
                    <toolkit:SettingsCard>
                        <toolkit:SettingsCard.Header>
                            <StackPanel Orientation="Horizontal" Spacing="12" VerticalAlignment="Center">
                                <PersonPicture ProfilePicture="/Assets/Avatars/gabin.png"
                                               Width="40" Height="40" />
                                <TextBlock Text="김가빈" VerticalAlignment="Center" />
                            </StackPanel>
                        </toolkit:SettingsCard.Header>
                        <TextBlock Text="피드백은 받지 않겠습니다. 감사합니다." />
                    </toolkit:SettingsCard>

                    <toolkit:SettingsCard>
                        <toolkit:SettingsCard.Header>
                            <StackPanel Orientation="Horizontal" Spacing="12" VerticalAlignment="Center">
                                <PersonPicture ProfilePicture="/Assets/Avatars/thyun.png"
                                                   Width="40" Height="40" />
                                <TextBlock Text="김태현" VerticalAlignment="Center" />
                            </StackPanel>
                        </toolkit:SettingsCard.Header>
                        <TextBlock Text="추가하고 싶은 기능이 있다면 언제든지 마음 속으로 말씀해주세요. " />
                    </toolkit:SettingsCard>

                    <TextBlock Style="{StaticResource SettingsSectionHeaderTextBlockStyle}" Text="오픈소스 라이브러리" />
                    <toolkit:SettingsCard Header="Windows Community Toolkit" Description="MIT License. UI 컨트롤 및 헬퍼 라이브러리.">
                        <toolkit:SettingsCard.HeaderIcon>
                            <FontIcon Glyph="&#xE8A1;" />
                        </toolkit:SettingsCard.HeaderIcon>
                        <HyperlinkButton Content="자세히 보기" NavigateUri="https://github.com/CommunityToolkit/WindowsCommunityToolkit"/>
                    </toolkit:SettingsCard>
                    
                    <toolkit:SettingsCard Header="Newtonsoft.Json" Description="MIT License. JSON 직렬화 라이브러리.">
                        <toolkit:SettingsCard.HeaderIcon>
                            <FontIcon Glyph="&#xF16B;" />
                        </toolkit:SettingsCard.HeaderIcon>
                        <HyperlinkButton Content="자세히 보기" NavigateUri="https://www.newtonsoft.com/json"/>
                    </toolkit:SettingsCard>
                    
                </StackPanel>
            </Border>
        </ScrollView>
    </Grid>
</Page>
